apiVersion: v1
kind: Service
metadata:
  name: {{ include "posthorn.fullname" . }}
  labels:
    {{- include "posthorn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "posthorn.selectorLabels" . | nindent 4 }}
