# Builder Brief - F-0032: Code-verified parity matrix

> Immutable. Build exactly this; report back; nothing more. Explorer fan-out was skipped because the gap came from direct repo inspection plus live official-doc research: `README.md` links to `docs/PARITY.md`, but that file is missing. The matrix must be useful and honest, not a marketing page.

## Goal

Add `docs/PARITY.md`, the code-verified competitor matrix promised by the README. It should compare current Posthorn with comparable webhook products, show where Posthorn is already strong, and list the short codeable gaps without claiming unsupported scale-out, transformations, deduplication rules, or destination connectors.

## Current Gap

- `README.md` already links to `docs/PARITY.md` under "Why Posthorn".
- `docs/PARITY.md` does not exist.
- Root `GOAL.md` also mentions the parity matrix, but it contains stale pre-purge lifecycle text and is outside this feature unless a specific README-facing consistency edit is needed.
- `docs/GOAL.md`, `docs/ai/REPO_MAP.md`, `tickets/`, and `scripts/agent/` are also absent from the current checkout; do not broaden this feature into general legacy-doc cleanup.

## Official Source Set

Use current official docs only for competitor claims:

- Svix:
  - https://docs.svix.com/quickstart
  - https://docs.svix.com/retries
  - https://docs.svix.com/throttling
  - https://docs.svix.com/app-portal
  - https://docs.svix.com/event-types
  - https://docs.svix.com/transformations
- Convoy:
  - https://www.getconvoy.io/
  - https://www.getconvoy.io/product-manual/endpoints
  - https://www.getconvoy.io/product-manual/webhooks-documentation
  - https://www.getconvoy.io/product-manual/portal-links
  - https://github.com/frain-dev/convoy
- Hookdeck:
  - https://hookdeck.com/docs/hookdeck-basics
  - https://hookdeck.com/docs/connections
  - https://hookdeck.com/docs/retries
  - https://hookdeck.com/docs/use-cases/receive-webhooks
  - https://hookdeck.com/docs/outpost/concepts
- Stripe:
  - https://docs.stripe.com/webhooks
  - https://docs.stripe.com/workbench/event-destinations
  - https://docs.stripe.com/api/webhook_endpoints/object
  - https://docs.stripe.com/api/idempotent_requests

Avoid pricing claims and broad "best" rankings. If a competitor feature is not clearly in one of the official sources above, leave it out.

## Posthorn Evidence Map

Tie supported Posthorn rows to local implemented evidence:

- Single-container SQLite/no Redis: `README.md`, `Dockerfile`, `docker-compose.yml`, `tests/deployment-artifacts.test.ts`.
- Standard Webhooks signing/verification: `README.md`, `tests/webhooks.test.ts`.
- Endpoint CRUD, one-time secrets, rotation, and admin provisioning: `README.md`, `tests/endpoints-http.test.ts`, `tests/admin-http.test.ts`.
- Message intake, batch intake, idempotency, retry worker, dead-lettering, and manual retry: `README.md`, `tests/messages-http.test.ts`, `tests/worker.test.ts`.
- Per-attempt audit, delivery history/stats, message filters, and app-wide delivery listing: `README.md`, relevant `tests/*http.test.ts` files.
- Event type catalog, endpoint test-send, and portal sessions: `README.md`, `tests/event-types-http.test.ts`, `tests/endpoint-test-http.test.ts`, `tests/portal-sessions-http.test.ts`.
- Metrics, alerts, dashboard artifacts, Docker, and Helm: `docs/DEPLOY.md`, `tests/monitoring-artifacts.test.ts`, `tests/deployment-artifacts.test.ts`, `tests/helm-chart.test.ts`.

## Required Doc Shape

Create `docs/PARITY.md` with:

1. A short purpose paragraph: source-backed comparison as of 2026-06-12, checked against current Posthorn code/tests.
2. A legend such as `Implemented`, `Partial`, `Not yet`, and `Out of scope`.
3. A compact matrix comparing Posthorn, Svix, Convoy, Hookdeck, and Stripe across core webhook infrastructure rows:
   - outbound webhook delivery
   - durable retry queue / at-least-once delivery
   - message intake idempotency
   - endpoint CRUD and event filtering
   - endpoint signing secrets and rotation
   - manual retry / replay
   - per-attempt logs and delivery search
   - event type catalog
   - customer or tenant portal
   - endpoint test-send
   - endpoint delivery throttling / rate limiting
   - metrics/alerts/operator artifacts
   - self-hosted single-container mode
   - Kubernetes reference
   - PostgreSQL/HA scale-out
   - payload transformations
   - deduplication rules beyond intake idempotency
   - non-webhook destination connectors
4. A "Posthorn gaps" section with codeable next work:
   - PostgreSQL/HA scale-out.
   - Payload transformations.
   - Configurable deduplication rules beyond producer idempotency.
   - Non-webhook destination connectors.
5. A "Not counted as code gaps" section for human-only commercial/release work if needed, but do not invent credential-gated tasks.
6. A "Sources" section with the official URLs above.

Do not include secret-shaped sample values (`phk_`, `whsec_`, `POSTHORN_ADMIN_TOKEN=`) or unsourced pricing.

## Tests

Add `tests/parity-doc.test.ts`:

- Assert `docs/PARITY.md` exists and README links to it.
- Assert the doc names Posthorn, Svix, Convoy, Hookdeck, and Stripe.
- Assert the doc includes the required capability rows and "Posthorn gaps" section.
- Assert the source URLs above appear in the doc.
- Assert it contains the current boundary terms: "PostgreSQL/HA scale-out", "Payload transformations", "Deduplication rules beyond intake idempotency", and "Non-webhook destination connectors".
- Assert it does not contain secret-shaped samples, `POSTHORN_ADMIN_TOKEN=`, unsupported claims like `Posthorn | Implemented | Payload transformations`, or pricing dollar amounts.

## Verification

Run:

- `npx vitest run tests/parity-doc.test.ts`
- `npm run typecheck`
- `npm test`
- `npm run lint`
- `npm run build`
- `npx ts-node scripts/update-state.ts --validate`
- `git diff --check`
- `npx ts-node scripts/assertion-shield.ts`

Security review can be skipped if the final diff stays docs/test/state only and contains no secrets, dependencies, runtime code, auth, API, data, or workflow changes. Evaluator review is still required.
