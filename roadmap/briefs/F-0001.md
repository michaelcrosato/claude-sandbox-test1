# Builder Brief - F-0001: Product TypeScript scaffold and gate wiring

> Immutable. Build exactly this; report back; nothing more. Explorer fan-out was skipped in this Codex session because the available sub-agent tool is restricted to explicit user-requested delegation. Repo context was gathered directly from tracked files.

## Feature
Create the runnable Node/TypeScript product skeleton for Posthorn and wire it into the existing operations gate. This is the first product feature from `roadmap/ROADMAP.md#now`; it converts the repo from template-only mode into product mode while preserving the guardrail layer.

## Spec
Current state: the repository has the AI operations engine, package metadata, and product specification, but no `src/` product tree. `scripts/verify.sh` already flips into stricter product mode when `src/` exists, requiring lint and unit tests. Build a minimal but real TypeScript product scaffold:

- Add product source under `src/`.
- Export a minimal Posthorn entry point from `src/index.ts`.
- Include a small gateway factory shape that later features can extend without rewriting the scaffold.
- Add a Vitest test proving the entry point can be imported and the minimal gateway shape is usable.
- Add npm scripts for `test` and `build`; keep `typecheck`, `lint`, and `verify` working.
- Update TypeScript config so product source and tests are typechecked/built correctly.
- Update README development/status wording so it is clear the mature route list is the target product spec and the current implementation is at scaffold stage.
- Do not edit hooks, workflows, or engine scripts.

## Acceptance criteria (verbatim from roadmap/features.json)
1. npm scripts named typecheck, test, build, and lint exist and succeed on a clean checkout.
2. bash scripts/verify.sh runs in product mode and passes without template-mode skips for lint or unit tests.
3. src/index.ts exports a minimal Posthorn product entry point and a unit test proves it can be imported.
4. README development instructions match the scaffold and do not claim unimplemented routes are available.

## Your Scope & Rules
- Touch ONLY: `package.json`, `package-lock.json`, `tsconfig.json`, `biome.json`, `vitest.config.ts`, `src/**`, `tests/**`, `README.md`, `roadmap/features.json`, `roadmap/briefs/F-0001.md`, `roadmap/evidence/F-0001/**`, `roadmap/PROGRESS.md`, `roadmap/DECISIONS.md`, `roadmap/metrics.jsonl`, `roadmap/STATUS.md`.
- Authorized paths: `package.json`, `package-lock.json`, `tsconfig.json`, `biome.json`, `vitest.config.ts`, `src/**`, `tests/**`, `README.md`, `roadmap/features.json`, `roadmap/briefs/F-0001.md`, `roadmap/evidence/F-0001/**`, `roadmap/PROGRESS.md`, `roadmap/DECISIONS.md`, `roadmap/metrics.jsonl`, `roadmap/STATUS.md`. Forbidden: `.claude/**`, `.github/workflows/**`, `scripts/**`.
- Run `bash scripts/verify.sh` and save the full output to `roadmap/evidence/F-0001/verify.log`.
- Exit code is the only truth: report success only on `VERIFY: PASS (exit 0)`.
- Never delete, weaken, or skip any assertion. Work silently; final report = files changed, evidence paths, verify exit code, anything surprising.
- Applicable `.claude/rules/`: none for this scaffold.
